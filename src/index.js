import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';

ReactDOM.render(
  <React.StrictMode>
    {function App() {
        return (
          <div className="App">
          </div>
        );
      }
    }
  </React.StrictMode>,
  document.getElementById('root')
);
